[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Google Sheets API keys are designed for client-side use with API restrictions
  # They are intentionally embedded in the Next.js public bundle
  SECRETS_SCAN_ENABLED = "false"
